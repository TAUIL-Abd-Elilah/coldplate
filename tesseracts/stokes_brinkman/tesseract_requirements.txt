# Pinned to the versions these results were produced and validated with. If a
# pin becomes unavailable, update it only together with a fresh validation run.
#
# No AD library and no linear-algebra package: the solver and its hand-derived
# adjoint are entirely inside the C++/Eigen shared library. numpy is only used
# to marshal arrays across the ctypes boundary.
numpy==2.4.6
